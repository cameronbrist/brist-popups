# Used only by scripts/ (never deployed). Copy to .env.admin.
# Create a custom app in the pop-up store admin with scopes:
#   write_metaobject_definitions, write_metaobjects, read_products
SHOPIFY_STORE_DOMAIN="brist-popups.myshopify.com"
SHOPIFY_ADMIN_TOKEN="shpat_..."
SHOPIFY_ADMIN_API_VERSION="2026-04"
