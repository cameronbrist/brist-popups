# Local-only variables for MiniOxygen. Run `npx shopify hydrogen link` then
# `npx shopify hydrogen env pull` to pull real values from the Plus store.
SESSION_SECRET="local-dev-secret-change-me"
POPUP_USE_FIXTURES="true"
POPUP_DEFAULT_HANDLE="sample-quiet"
POPUP_ALLOW_OVERRIDE="true"
